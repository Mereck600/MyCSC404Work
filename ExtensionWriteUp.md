# Extension 5 

I began with option 2. I knew that to add getters and setters, I would only need to change the parser. In classDeclerations, i changed the wihle loop to a new method. This allows fro broader class definitions. The method I added "method()" which will either parse as normal if there are parameters, those will be parsed, but otherwise, we can assume it is a getter method. See test for example. Chatgpt built the test. "build a test case for a lox getter methods"

Option 3: 